name: Deploy to Azure Web App

on:
  push:
    branches: [ "main" ]
    paths:
      - '**/*.ts'
      - '**/*.tsx'
      - 'package.json'
      - 'yarn.lock'
      - 'next.config.js'
      - 'public/**'
      - 'src/**'
  workflow_dispatch: {}

env:
  AZURE_WEBAPP_NAME: forwardapp
  AZURE_RG: coe-platform-engineering-rg
  AZURE_REGION: southeastasia

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      artifact-name: next-build
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: 'yarn'

      - name: Install Dependencies
        run: yarn install --frozen-lockfile

      - name: Build Next.js
        env:
          NODE_OPTIONS: --no-node-snapshot
        run: yarn build

      - name: Export (static) if applicable
        if: false # set to true if you use "next export"
        run: yarn next export

      - name: Archive for Deploy
        run: |
          tar -czf app.tar.gz .next package.json yarn.lock next.config.js public
      
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: next-build
          path: app.tar.gz

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment: production
    env:
      # Secrets are exposed as environment variables here for the 'az login' step
      AZ_CLIENT_ID: ${{ secrets.AZ_CLIENT_ID }}
      AZ_CLIENT_SECRET: ${{ secrets.AZ_CLIENT_SECRET }}
      AZ_SUBSCRIPTION_ID: ${{ secrets.AZ_SUBSCRIPTION_ID }}
      AZ_TENANT_ID: ${{ secrets.AZ_TENANT_ID }}
    permissions:
      contents: read
      # The 'id-token: write' permission is removed as it's not needed for secret-based login.

    steps:
      - name: Download Build Artifact
        uses: actions/download-artifact@v4
        with:
          name: next-build
          path: .

      - name: Unpack Artifact
        # This makes the app.tar.gz file available, along with the unpacked contents.
        run: tar -xzf app.tar.gz

      - name: Azure CLI Login
        # Log in using the Service Principal secrets
        run: |
          az login --service-principal \
            --username "$AZ_CLIENT_ID" \
            --password "$AZ_CLIENT_SECRET" \
            --tenant "$AZ_TENANT_ID"
          az account set --subscription "$AZ_SUBSCRIPTION_ID"

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ env.AZURE_WEBAPP_NAME }}
          # FIX: Specify the exact name of the archived file.
          package: app.tar.gz 
          slot-name: production 

      - name: Restart Web App (Finalize Deployment)
        run: az webapp restart --name "$AZURE_WEBAPP_NAME" --resource-group "$AZURE_RG"
